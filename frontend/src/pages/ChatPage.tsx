import { PagePlaceholder } from "../components/pages/PagePlaceholder";

export function ChatPage() {
  return (
    <PagePlaceholder
      title="开始咨询"
      description="这里将作为未来 AI 咨询服务入口。当前 Sprint 只完成路由和页面结构。"
    />
  );
}
